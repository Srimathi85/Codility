/*
* 15. Implement minimum priority queue to store student information. Each student has student name(string)and
* student id number(int) and priority is nothing but student id. Implement the following methods
* EnQueue()-adds student info to queue which maintains order of minimum priority DeQueue()-
*  which deletes minimum id holder information peekMin() which returns student information having
* minimum id value
 */
public class Q15 {
}
